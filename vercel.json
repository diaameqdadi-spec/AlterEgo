{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "if [ -f package.json ]; then npm install; else cd frontend && npm install; fi",
  "buildCommand": "if [ -f package.json ]; then npm run build; else cd frontend && npm run build; fi",
  "devCommand": "if [ -f package.json ]; then npm run dev; else cd frontend && npm run dev; fi"
}
